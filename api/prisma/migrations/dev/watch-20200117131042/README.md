# Migration `watch-20200117131042`

This migration has been generated by Conner Novicki at 1/17/2020, 1:10:42 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "quaint"."User" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" INTEGER NOT NULL   ,
  "role" TEXT NOT NULL DEFAULT 'USER'  ,
  "username" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "quaint"."Restaurant" (
  "creator" INTEGER NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT,
  "id" INTEGER NOT NULL   ,
  PRIMARY KEY ("id")
);

CREATE TABLE "quaint"."Review" (
  "author" INTEGER NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT,
  "id" INTEGER NOT NULL   ,
  "rating" INTEGER NOT NULL DEFAULT 0  ,
  "restaurant" INTEGER NOT NULL   REFERENCES "Restaurant"(id) ON DELETE RESTRICT,
  PRIMARY KEY ("id")
);

CREATE TABLE "quaint"."Comment" (
  "author" INTEGER NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" INTEGER NOT NULL   ,
  "review" INTEGER    REFERENCES "Review"(id) ON DELETE SET NULL,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration watch-20200117124056..watch-20200117131042
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 enum Role {
   USER
@@ -13,8 +13,33 @@
   ADMIN
 }
 model User {
-  id    String  @default(cuid()) @id
-  username String
-  role Role
+  id               Int          @id
+  username         String
+  role             Role         @default(USER)
+  ownedRestaurants Restaurant[]
+  authoredReviews  Review[]
+  authoredComments Comment[]
+  createdAt        DateTime     @default(now())
 }
+
+model Restaurant {
+  id      Int      @id
+  creator User
+  reviews Review[]
+}
+
+model Review {
+  id       Int       @id
+  rating   Int
+  restaurant Restaurant
+  author   User
+  comments Comment[]
+}
+
+model Comment {
+  id        Int      @id
+  author    User
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
```


