# Migration `watch-20200117132725`

This migration has been generated by Conner Novicki at 1/17/2020, 1:27:25 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_User" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" INTEGER NOT NULL   ,
  "role" TEXT NOT NULL DEFAULT 'USER'  ,
  "username" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_User" ("createdAt","id","role","username") SELECT "createdAt","id","role","username" from "User"

DROP TABLE "quaint"."User";

ALTER TABLE "quaint"."new_User" RENAME TO "User";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Restaurant" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "creator" INTEGER NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT,
  "dateOfVisit" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" INTEGER NOT NULL   ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Restaurant" ("creator","id") SELECT "creator","id" from "Restaurant"

DROP TABLE "quaint"."Restaurant";

ALTER TABLE "quaint"."new_Restaurant" RENAME TO "Restaurant";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Comment" (
  "author" INTEGER NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" INTEGER NOT NULL   ,
  "review" INTEGER    REFERENCES "Review"(id) ON DELETE SET NULL,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Comment" ("author","createdAt","id","review","updatedAt") SELECT "author","createdAt","id","review","updatedAt" from "Comment"

DROP TABLE "quaint"."Comment";

ALTER TABLE "quaint"."new_Comment" RENAME TO "Comment";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration watch-20200117131042..watch-20200117132725
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 enum Role {
   USER
@@ -23,19 +23,21 @@
   createdAt        DateTime     @default(now())
 }
 model Restaurant {
-  id      Int      @id
-  creator User
-  reviews Review[]
+  id               Int      @id
+  creator          User
+  reviews          Review[]
+  dateOfVisit      DateTime
+  createdAt        DateTime     @default(now())
 }
 model Review {
-  id       Int       @id
-  rating   Int
+  id         Int        @id
+  rating     Int
   restaurant Restaurant
-  author   User
-  comments Comment[]
+  author     User
+  comments   Comment[]
 }
 model Comment {
   id        Int      @id
```


