# Migration `watch-20200121131432`

This migration has been generated by Conner Novicki at 1/21/2020, 1:15:20 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Review" (
  "author" TEXT NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT,
  "dateOfVisit" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "rating" INTEGER NOT NULL DEFAULT 0  ,
  "restaurant" TEXT NOT NULL   REFERENCES "Restaurant"(id) ON DELETE RESTRICT,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Review" ("author","dateOfVisit","id","rating","restaurant") SELECT "author","dateOfVisit","id","rating","restaurant" from "Review"

DROP TABLE "quaint"."Review";

ALTER TABLE "quaint"."new_Review" RENAME TO "Review";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_User" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "role" TEXT NOT NULL DEFAULT 'USER'  ,
  "username" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_User" ("createdAt","id","role","username") SELECT "createdAt","id","role","username" from "User"

DROP TABLE "quaint"."User";

ALTER TABLE "quaint"."new_User" RENAME TO "User";

CREATE UNIQUE INDEX "quaint"."User.username" ON "User"("username")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Restaurant" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "description" TEXT    ,
  "id" TEXT NOT NULL   ,
  "name" TEXT NOT NULL DEFAULT ''  ,
  "owner" TEXT NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Restaurant" ("createdAt","description","id","name","owner") SELECT "createdAt","description","id","name","owner" from "Restaurant"

DROP TABLE "quaint"."Restaurant";

ALTER TABLE "quaint"."new_Restaurant" RENAME TO "Restaurant";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Comment" (
  "author" TEXT NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "review" TEXT    REFERENCES "Review"(id) ON DELETE SET NULL,
  "text" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Comment" ("author","createdAt","id","review","text","updatedAt") SELECT "author","createdAt","id","review","text","updatedAt" from "Comment"

DROP TABLE "quaint"."Comment";

ALTER TABLE "quaint"."new_Comment" RENAME TO "Comment";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration watch-20200121131357..watch-20200121131432
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 enum Role {
   USER
@@ -27,9 +27,9 @@
   id          String   @default(cuid()) @id
   name        String
   description String?
   owner       User
-  reviews     Review[]
+  reviews     Review[] @relation(onDelete: CASCADE)
   createdAt   DateTime @default(now())
 }
 model Review {
@@ -37,9 +37,9 @@
   rating      Int
   restaurant  Restaurant
   author      User
   dateOfVisit DateTime
-  comments    Comment[]
+  comments    Comment[] @relation(onDelete: CASCADE)
 }
 model Comment {
   id        String   @default(cuid()) @id
```


